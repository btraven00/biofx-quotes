# Quotes from our own lab: professors, PIs, and the occasional grad student.
# Add one per entry, separated by a line containing only "%".
# Attribute with a tab-indented "-- Name" line, like the examples below.
# Lines starting with "#" are NOT special to fortune -- they will be shown!
# So delete this comment block once you start adding real quotes.
%
"Just align it to the genome."
		-- every PI, eventually
%
"The error is almost certainly in your code, not in the tool."
		-- our PI, who was usually right
%
Single-cell analysis with simulated data is,
at best, Mickey Mouse science.
		-- W.H.
%
